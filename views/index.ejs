<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("partials/header") %>
    <title>Home</title>
  </head>
  <body>
    <%- include ("partials/nav") %>
    <div class="p-5 mb-4 bg-light rounded-3">
      <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold">學習筆記</h1>
        <p class="col-md-8 fs-4">按照這個月以來實作的專案的紀錄</p>
        <button class="btn btn-primary btn-lg" type="button">
          <a href="/auth/signup" style="color: white; text-decoration: none"
            >SIGN UP</a
          >
        </button>
      </div>
    </div>
    <!-- post -->
    <% if(posts.length>0) { for (let i=0 ; i < posts.length; i++) {%>
    <div class="card" style="margin: 16px 8px">
      <!-- 圖片 -->
      <!-- <img src="note_image\Frame+1+-+複製.png" class="card-img-top" alt="..."> -->
      <div class="card-header">
        <h5 class="card-title">Title: <%= posts[i].title %></h5>
      </div>
      <ul class="list-group list-group-flush">
        <li class="list-group-item">
          Project: <a href="<%= posts[i].project %>" class="card-text">URL</a>
        </li>
        <li class="list-group-item">Date: <%= posts[i].date %></li>
      </ul>
      <div class="card-body">
        <p class="card-text">Content: <%= posts[i].content %></p>
      </div>
    </div>
    <% } %> <% } %>
  </body>
</html>
